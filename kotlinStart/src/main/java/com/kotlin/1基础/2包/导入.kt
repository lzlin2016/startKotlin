package com.kotlin.`1基础`.`2包`

/**
 * 项目名称: StartKotlin
 *
 * 类的描述: 导包
 * 创建时间:  2017/7/19 17:52
 * 修改备注:
 *      1. 有多个包会默认导入到每个 Kotlin 文件中：
 *      2. 可以导入一个单独的名字, 也可以导入一个作用域下的所有内容（包、类、对象等）
 *          import foo.Bar // 现在 Bar 可以不用限定符访问
 *          import foo.* // “foo”中的一切都可访问
 *      3. 如果出现名字冲突，可以使用 as 关键字在本地重命名冲突项来消歧义：
 *          import foo.Bar // Bar 可访问
 *          import bar.Bar as bBar // bBar 代表“bar.Bar”
 *      4. 关键字 import 并不仅限于导入类；也可用它来导入其他声明：所有这些声明都用 import 关键字导入
 *      5. 如果顶层声明是 private 的，它是声明它的文件所私有的
 */
fun main(args: Array<String>) {

}